<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.liteup">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserFoodActivity"></activity>
        <activity android:name=".UserRestuarantActivity" />
        <activity
            android:name=".FoodListActivity"
            android:label="Food List"
            android:parentActivityName=".AddFoodActivity" />
        <activity android:name=".AddFoodActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".AdminHome" />
        <activity android:name=".UpdateUser" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".AdminLogIn" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ModifyRestaurantActivity" />
        <activity android:name=".AddRestaurantActivity" />
        <activity android:name=".RestaurentListActivity" />
    </application>

</manifest>